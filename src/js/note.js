export default class Note {
    constructor(key, sampleNumber, effectNumber, effectParameter) {
        this.key = key;
        this.sampleNumber = sampleNumber;
        this.effectNumber = effectNumber;
        this.effectParameter = effectParameter;
    }
}
